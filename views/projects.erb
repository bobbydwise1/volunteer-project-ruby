<div class="container">
  <div class="row">
    <div class="col container">
      <h1>Projects</h1>

      <div class="container">
        <h5>The name of the current project is:</h5>
        <p><b>id: </b><%=@current_project.id%></p>
        <p><b>title: </b><%=@current_project.title%></p>

      </div>

      <hr></hr>

      <div>

        <a href="/projects/<%=@current_project.id%>/edit">Edit Project</a>

      </div>

      <hr></hr>

      <div>
        <ul>
          <% if @current_volunteers != nil %>
              <% @currernt_volunteers.each do |volunteer| %>
                  <li>
                    <a href="/volunteers/<%= volunteer.id %>"><%= volunteer.id %>, <%= volunteer.name %>,
                      <% if volunteer.project_id != 0 %>
                      <%= volunteer.project_id %>
                      <% else %>
                      No project assigned
                      <% end %>
                    </a>
                  </li>
              <% end %>
          <% end %>
        </ul>
      </div>

      <hr></hr>

      <a href="/">Index</a>

    </div>
  </div>
</div>
